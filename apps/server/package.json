{
  "name": "@rb/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Riftbound Online server: accounts, rooms and authoritative matches.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "dev": "node --experimental-strip-types --watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.0.2",
    "@rb/engine": "*",
    "@rb/protocol": "*",
    "drizzle-orm": "^0.44.5",
    "fastify": "^5.6.0",
    "@fastify/rate-limit": "^10.3.0",
    "pino": "^9.9.0",
    "postgres": "^3.4.7",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.10",
    "drizzle-kit": "^0.31.4"
  }
}
